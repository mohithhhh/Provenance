# Copy to .env.local to override. Defaults to http://localhost:8000, which
# matches `uvicorn app.main:app --reload`'s default port — most local dev
# doesn't need this file at all. Docker Compose sets this itself.
NEXT_PUBLIC_API_URL=http://localhost:8000
