fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0

# Module F (retrieval provenance ledger): ONNX-based embeddings, not
# sentence-transformers/torch — same real semantic embedding quality for a
# fraction of the disk footprint (see docs/architecture.md).
fastembed>=0.8.0,<0.9.0
numpy>=2.0.0,<3.0.0

# Module B (zero-shot statistical detector): real small language models
# (gpt2/distilgpt2) for perplexity/cross-perplexity/GLTR — there's no
# lightweight substitute for this one, unlike Module F's embeddings.
torch>=2.10.0,<3.0.0
transformers>=5.0.0,<6.0.0

# Module C (trained classifier): logistic regression + standardization over
# hand-picked stylometric features. No torch/transformers needed here —
# the feature set is small and simple enough that a classical model is the
# right, interpretable choice (see docs/architecture.md).
scikit-learn>=1.5.0,<2.0.0

# Module D (file provenance): official C2PA SDK (Rust c2pa-rs bindings) for
# manifest verification, and Pillow for EXIF — the one piece of metadata
# extraction with no stdlib equivalent, and by far the most common way to
# read it in Python.
c2pa-python>=0.37.0,<0.38.0
Pillow>=10.0.0,<13.0.0

# Needed for FastAPI/Starlette's multipart file-upload parsing (Module D's
# /provenance/file endpoint is this project's first file-upload route).
python-multipart>=0.0.12,<1.0.0

# Module G (Attack Lab): the T5 paraphrase model's tokenizer is
# SentencePiece-based (spiece.model) — without this, transformers falls
# back to a tiktoken-format loader that can't read it at all.
sentencepiece>=0.2.0,<0.3.0
