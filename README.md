# Provenance
AI-content detection suite that goes past stylometry: an original watermark implementation, a semantic retrieval ledger built specifically to survive paraphrasing (the attack that collapses most detectors), and a live Attack Lab that red-teams every method in the open — reporting calibrated, disagreement-aware results instead of one black-box score

currently in DEV
